# Node.js app image template for per-project isolation
FROM node:20-alpine

WORKDIR /app

# Install dependencies first for layer caching
COPY package*.json ./
RUN npm ci --only=production || npm install

COPY . .

EXPOSE 3000
CMD ["npm", "start"]
